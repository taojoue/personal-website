import React from 'react'
// import Navbar from './navbar'

/* omment */
const Layout = ({ children }) => {
    return (
        <div>
            {/* <Navbar />
            <main>
                {children}
            </main>
            <Footer/> */}
        </div>
    )
}

export default Layout